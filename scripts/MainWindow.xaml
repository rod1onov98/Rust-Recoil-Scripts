<Window x:Class="RustRecoilControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CT SCRIPTS - Rust Recoil Control"
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#1B1B1B">

    <Window.Resources>
        <DropShadowEffect x:Key="GlowEffect" BlurRadius="15" ShadowDepth="0" Color="#00BFFF" Opacity="0.8"/>
        <DropShadowEffect x:Key="SubtleGlowEffect" BlurRadius="8" ShadowDepth="0" Color="#00BFFF" Opacity="0.3"/>

        <SolidColorBrush x:Key="BackgroundColor" Color="#1B1B1B"/>
        <SolidColorBrush x:Key="DarkerBackgroundColor" Color="#141414"/>
        <SolidColorBrush x:Key="PanelColor" Color="#2B2B2B"/>
        <SolidColorBrush x:Key="LighterPanelColor" Color="#333333"/>
        <SolidColorBrush x:Key="AccentColor" Color="#00BFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="LightTextColor" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="BorderColor" Color="#444444"/>
        <SolidColorBrush x:Key="SelectedTabColor" Color="#262626"/>

        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource LightTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="0" 
                                Padding="15,10"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SelectedTabColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LighterPanelColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource LighterPanelColor}"/>
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="btnBorder" Property="Effect" Value="{StaticResource SubtleGlowEffect}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{StaticResource LightTextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource LighterPanelColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Border Background="#252525" Height="45" VerticalAlignment="Top" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <DockPanel>
                <TextBlock Text="CT SCRIPTS" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="18" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Style="{StaticResource WindowControlButton}" Content="_" Click="MinimizeButton_Click"/>
                    <Button Style="{StaticResource WindowControlButton}" Content="X" Click="CloseButton_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Margin="0,45,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource DarkerBackgroundColor}" BorderThickness="0,0,1,0" BorderBrush="{StaticResource BorderColor}">
                <StackPanel Margin="0,50,0,0">
                    <RadioButton x:Name="WeaponTab" Content="Weapon Selection" GroupName="Tabs" Style="{StaticResource TabButtonStyle}" IsChecked="True"/>
                    <RadioButton x:Name="AttachmentsTab" Content="Attachments" GroupName="Tabs" Style="{StaticResource TabButtonStyle}"/>
                    <RadioButton x:Name="RecoilTab" Content="Recoil Control" GroupName="Tabs" Style="{StaticResource TabButtonStyle}"/>
                    <RadioButton x:Name="SensitivityTab" Content="Sensitivity" GroupName="Tabs" Style="{StaticResource TabButtonStyle}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource BackgroundColor}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#252540" Padding="15">
                        <TextBlock Text="CT SCRIPTS"
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Effect="{StaticResource GlowEffect}"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="15">

                            <StackPanel x:Name="WeaponPanel" Margin="5">
                                <GroupBox Header="Weapon Selection">
                                    <Border Background="{StaticResource PanelColor}" CornerRadius="4" Margin="0,10,0,0" Padding="10">
                                        <StackPanel>
                                            <ComboBox x:Name="WeaponComboBox" Margin="5" SelectionChanged="WeaponComboBox_SelectionChanged"
                                                      Background="{StaticResource PanelColor}"
                                                      Foreground="{StaticResource TextColor}"
                                                      BorderBrush="{StaticResource BorderColor}">
                                                <ComboBox.ItemContainerStyle>
                                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboBoxItemStyle}"/>
                                                </ComboBox.ItemContainerStyle>
                                            </ComboBox>
                                            <CheckBox x:Name="EnableCheckBox" Content="Enable Recoil Control" Margin="5" Checked="EnableCheckBox_Checked" Unchecked="EnableCheckBox_Unchecked"/>
                                        </StackPanel>
                                    </Border>
                                </GroupBox>
                            </StackPanel>

                            <StackPanel x:Name="AttachmentsPanel" Margin="5" Visibility="Collapsed">
                                <GroupBox Header="Attachments">
                                    <Border Background="{StaticResource PanelColor}" CornerRadius="4" Margin="0,10,0,0" Padding="10">
                                        <StackPanel>
                                            <TextBlock Text="Sight" FontWeight="SemiBold"/>
                                            <ComboBox x:Name="SightComboBox" Margin="5,5,5,10" SelectionChanged="SightComboBox_SelectionChanged"
                                                      Background="{StaticResource PanelColor}"
                                                      Foreground="{StaticResource TextColor}"
                                                      BorderBrush="{StaticResource BorderColor}">
                                                <ComboBox.ItemContainerStyle>
                                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboBoxItemStyle}"/>
                                                </ComboBox.ItemContainerStyle>
                                            </ComboBox>
                                            <TextBlock x:Name="SightModifierText" Text="+0%" HorizontalAlignment="Center" Foreground="{StaticResource AccentColor}" Margin="0,0,0,15"/>

                                            <TextBlock Text="Muzzle Attachment" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                            <ComboBox x:Name="MuzzleComboBox" Margin="5,5,5,10" SelectionChanged="MuzzleComboBox_SelectionChanged"
                                                      Background="{StaticResource PanelColor}"
                                                      Foreground="{StaticResource TextColor}"
                                                      BorderBrush="{StaticResource BorderColor}">
                                                <ComboBox.ItemContainerStyle>
                                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboBoxItemStyle}"/>
                                                </ComboBox.ItemContainerStyle>
                                            </ComboBox>
                                            <TextBlock x:Name="MuzzleModifierText" Text="+0%" HorizontalAlignment="Center" Foreground="{StaticResource AccentColor}"/>
                                        </StackPanel>
                                    </Border>
                                </GroupBox>
                            </StackPanel>

                            <StackPanel x:Name="RecoilPanel" Margin="5" Visibility="Collapsed">
                                <GroupBox Header="Recoil Control">
                                    <Border Background="{StaticResource PanelColor}" CornerRadius="4" Margin="0,10,0,0" Padding="10">
                                        <StackPanel>
                                            <TextBlock Text="Control X (Horizontal)" FontWeight="SemiBold"/>
                                            <Slider x:Name="ControlXSlider" Minimum="0" Maximum="100" Value="50" Margin="5" ValueChanged="ControlSlider_ValueChanged"/>
                                            <TextBlock x:Name="ControlXValue" Text="50%" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                                            <TextBlock Text="Control Y (Vertical)" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                            <Slider x:Name="ControlYSlider" Minimum="0" Maximum="100" Value="50" Margin="5" ValueChanged="ControlSlider_ValueChanged"/>
                                            <TextBlock x:Name="ControlYValue" Text="50%" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                                            <CheckBox x:Name="HipfireCheckBox" Content="Hipfire Mode" Margin="5" Checked="HipfireCheckBox_Checked" Unchecked="HipfireCheckBox_Checked"/>
                                        </StackPanel>
                                    </Border>
                                </GroupBox>
                            </StackPanel>

                            <StackPanel x:Name="SensitivityPanel" Margin="5" Visibility="Collapsed">
                                <GroupBox Header="Sensitivity">
                                    <Border Background="{StaticResource PanelColor}" CornerRadius="4" Margin="0,10,0,0" Padding="10">
                                        <StackPanel>
                                            <TextBlock Text="Normal Sensitivity" FontWeight="SemiBold"/>
                                            <Slider x:Name="SensitivitySlider" Minimum="0.01" Maximum="2.0" Value="1.0" Margin="5" ValueChanged="SensitivitySlider_ValueChanged"/>
                                            <TextBlock x:Name="SensitivityValue" Text="1.00" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                                            <TextBlock Text="ADS Sensitivity" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                            <Slider x:Name="AdsSensitivitySlider" Minimum="0.01" Maximum="2.0" Value="0.5" Margin="5" ValueChanged="AdsSensitivitySlider_ValueChanged"/>
                                            <TextBlock x:Name="AdsSensitivityValue" Text="0.50" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </GroupBox>
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>
                    
                    <Border Grid.Row="2" Background="#252540" Padding="10">
                        <TextBlock x:Name="StatusText" Text="Status: Waiting for Rust..." FontSize="14" HorizontalAlignment="Center" Effect="{StaticResource GlowEffect}"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
